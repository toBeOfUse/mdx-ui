# MDX UI Demo

This is a simple demo site for the MDX components library.

## Styles

I wanted to make sure that the MDX components work in a project that doesn't directly have TailwindCSS installed, so this site does not use Tailwind, and is instead slightly hacked together style-wise, with inline CSS and CSS modules.
