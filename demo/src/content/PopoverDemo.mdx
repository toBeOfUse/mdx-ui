import { MdxPopover } from '../../../'

<MdxPopover>

##### Click me for details

This is the popover content that appears when you click the button.
The `MdxPopover` finds an `<h5>` element among its children
and uses it as the button label. Everything else becomes the popover body.

</MdxPopover>

<MdxPopover>

##### Learn more about popovers

Popovers are great for showing additional context without navigating
away from the current page. They can contain **rich text**, lists, and
other Markdown content.

- Non-intrusive
- Dismissible
- Accessible by default

</MdxPopover>
